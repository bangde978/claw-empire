# CLImpire Configuration
PORT=8787
HOST=0.0.0.0

# Required for encrypting OAuth tokens at rest
OAUTH_ENCRYPTION_SECRET="change-me-to-a-long-random-string"

# Optional: override callback host for OAuth
# Tailscale 사용 시 MagicDNS 주소를 권장:
# OAUTH_BASE_URL="http://your-device-name.your-tailnet.ts.net:8787"

# GitHub OAuth (Settings → Developer settings → OAuth Apps)
# OAUTH_GITHUB_CLIENT_ID="..."
# OAUTH_GITHUB_CLIENT_SECRET="..."

# Google OAuth (Google Cloud Console → OAuth consent screen)
# OAUTH_GOOGLE_CLIENT_ID="..."
# OAUTH_GOOGLE_CLIENT_SECRET="..."
